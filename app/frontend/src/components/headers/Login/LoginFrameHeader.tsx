import styles from "./LoginFrameHeader.module.css";


export default function LoginFrameHeader() {
	return (
		<header className={styles.Header}>
			<div className={styles.Wrapper}/>
		</header>
	);
}