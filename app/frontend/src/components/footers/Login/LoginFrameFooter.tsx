import styles from "./LoginFrameFooter.module.css";

export default function LoginFrameFooter() {
	return (
		<footer className={styles.Footer}>
			<div className={styles.Wrapper} />
		</footer>
	);
}